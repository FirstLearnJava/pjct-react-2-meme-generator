/*const test = fetch('https://api.memegen.link/templates/')
  .then((response) => response.json())
  .then((data) => {
    const url = data.url; // extract the URL value from the JSON object
    console.log(url); // use the URL as needed
  })
  .catch((error) => {
    console.error('Error fetching JSON:', error);
  });
  */
